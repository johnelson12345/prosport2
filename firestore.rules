rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read/write access for now
    // You can customize this based on your security requirements
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
